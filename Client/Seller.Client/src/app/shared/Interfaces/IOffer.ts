export interface IOffer {
    id: string,
    listingTitle: string,
    listingId: string,
    price: number,
    created: string,
    creatorId: string,
    isAccepted: boolean,
    title: string
  }